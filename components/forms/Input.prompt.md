Labeled text field for dark forms (e.g. the "Solicitar demonstração" dialog).

```jsx
<Input label="E-mail profissional" type="email" required />
```

Pass any native input attribute (`required`, `placeholder`, `autoComplete`, `value`/`onChange`). Renders its own `<label>` wrapper; focus shows the green ring.
